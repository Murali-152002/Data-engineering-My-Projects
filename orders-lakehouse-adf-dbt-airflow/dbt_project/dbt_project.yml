name: 'orders_intelligence'
version: '1.0.0'
config-version: 2

profile: 'orders_intelligence'

model-paths: ["models"]
test-paths: ["tests"]

models:
  orders_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table
